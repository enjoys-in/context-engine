{
  "language": "azcli",
  "inlineCompletions": [
    {
      "triggerPattern": "^\\\\s*az group\\\\s*$",
      "insertText": "az group create --name ${1:myResourceGroup} --location ${2:eastus}",
      "description": "Create resource group",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*az vm\\\\s*$",
      "insertText": "az vm create --resource-group ${1:myResourceGroup} --name ${2:myVM} --image ${3:Ubuntu2204} --admin-username ${4:azureuser} --generate-ssh-keys",
      "description": "Create virtual machine",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*az account\\\\s*$",
      "insertText": "az account set --subscription ${1:subscriptionId}",
      "description": "Set active subscription",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*az webapp\\\\s*$",
      "insertText": "az webapp create --resource-group ${1:myResourceGroup} --plan ${2:myAppServicePlan} --name ${3:myWebApp} --runtime \"${4:NODE:18-lts}\"",
      "description": "Create web app",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*az storage\\\\s*$",
      "insertText": "az storage account create --name ${1:mystorageaccount} --resource-group ${2:myResourceGroup} --location ${3:eastus} --sku ${4:Standard_LRS}",
      "description": "Create storage account",
      "completeBracketPairs": true
    }
  ]
}