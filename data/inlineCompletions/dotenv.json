{
  "language": "dotenv",
  "inlineCompletions": [
    {
      "triggerPattern": "^\\\\s*DATABASE\\\\s*$",
      "insertText": "DATABASE_URL=${1:postgresql://user:password@localhost:5432/dbname}",
      "description": "Database URL variable",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*export\\\\s*$",
      "insertText": "export ${1:KEY}=${2:value}",
      "description": "Export variable",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*PORT\\\\s*$",
      "insertText": "PORT=${1:3000}",
      "description": "Port variable",
      "completeBracketPairs": true
    },
    {
      "triggerPattern": "^\\\\s*API\\\\s*$",
      "insertText": "API_KEY=${1:your_api_key_here}",
      "description": "API key variable",
      "completeBracketPairs": true
    },
    {
      "trigger": "for",
      "template": "for (let i = 0; i < ${1:length}; i++) {\n  ${0}\n}",
      "description": "For loop"
    },
    {
      "trigger": "if",
      "template": "if (${1:condition}) {\n  ${0}\n}",
      "description": "If statement"
    },
    {
      "trigger": "try",
      "template": "try {\n  ${0}\n} catch (${1:error}) {\n  \n}",
      "description": "Try-catch block"
    },
    {
      "trigger": "fn",
      "template": "function ${1:name}(${2:params}) {\n  ${0}\n}",
      "description": "Function declaration"
    },
    {
      "trigger": "log",
      "template": "console.log(${0})",
      "description": "Console log"
    },
    {
      "trigger": "imp",
      "template": "import { ${1:name} } from '${0:module}'",
      "description": "Import statement"
    },
    {
      "trigger": "class",
      "template": "class ${1:Name} {\n  constructor(${2:params}) {\n    ${0}\n  }\n}",
      "description": "Class declaration"
    },
    {
      "trigger": "switch",
      "template": "switch (${1:expr}) {\n  case ${2:val}:\n    ${0}\n    break;\n  default:\n    break;\n}",
      "description": "Switch statement"
    },
    {
      "trigger": "while",
      "template": "while (${1:condition}) {\n  ${0}\n}",
      "description": "While loop"
    },
    {
      "trigger": "map",
      "template": "${1:array}.map((${2:item}) => ${0})",
      "description": "Array map"
    },
    {
      "trigger": "filter",
      "template": "${1:array}.filter((${2:item}) => ${0})",
      "description": "Array filter"
    }
  ]
}
