{
  "language": "shell",
  "triggerCharacters": [" ", "("],
  "retriggerCharacters": [" "],
  "signatures": [
    {
      "label": "echo [option] [string ...]",
      "documentation": { "value": "Displays a line of text to standard output." },
      "parameters": [
        { "label": "option", "documentation": { "value": "Options: `-n` suppress trailing newline, `-e` enable backslash escapes, `-E` disable backslash escapes" } },
        { "label": "string", "documentation": { "value": "The string(s) to display" } }
      ]
    },
    {
      "label": "read [-p prompt] [-r] [-a array] [-d delim] [-t timeout] [name ...]",
      "documentation": { "value": "Reads a line from standard input and splits it into fields, assigning to variables." },
      "parameters": [
        { "label": "-p prompt", "documentation": { "value": "Display prompt before reading input" } },
        { "label": "-r", "documentation": { "value": "Do not treat backslash as an escape character" } },
        { "label": "-a array", "documentation": { "value": "Read into an indexed array variable" } },
        { "label": "name", "documentation": { "value": "Variable name(s) to assign the input fields to" } }
      ]
    },
    {
      "label": "printf format [arguments ...]",
      "documentation": { "value": "Formats and prints data according to a format string, similar to C printf." },
      "parameters": [
        { "label": "format", "documentation": { "value": "A format string with specifiers like %s, %d, %f" } },
        { "label": "arguments", "documentation": { "value": "Arguments to substitute into the format string" } }
      ]
    },
    {
      "label": "test expression | [ expression ]",
      "documentation": { "value": "Evaluate a conditional expression. Returns 0 (true) or 1 (false)." },
      "parameters": [
        { "label": "expression", "documentation": { "value": "The conditional expression, e.g. `-f file`, `-d dir`, `str1 = str2`, `n1 -eq n2`" } }
      ]
    },
    {
      "label": "export [-fn] [name[=value] ...]",
      "documentation": { "value": "Sets environment variables so they are available to child processes." },
      "parameters": [
        { "label": "name=value", "documentation": { "value": "Variable name and optional value to export" } },
        { "label": "-n", "documentation": { "value": "Remove the export property from the named variable" } }
      ]
    },
    {
      "label": "source filename [arguments]",
      "documentation": { "value": "Reads and executes commands from the given file in the current shell environment." },
      "parameters": [
        { "label": "filename", "documentation": { "value": "The file to source" } },
        { "label": "arguments", "documentation": { "value": "Positional parameters passed to the sourced script" } }
      ]
    },
    {
      "label": "trap [-lp] [action] [signal ...]",
      "documentation": { "value": "Defines actions to be taken when the shell receives a signal." },
      "parameters": [
        { "label": "action", "documentation": { "value": "A command string to execute when the signal is received, or `-` to reset" } },
        { "label": "signal", "documentation": { "value": "Signal name or number (e.g. SIGINT, EXIT, ERR, SIGTERM)" } }
      ]
    },
    {
      "label": "set [--abefhkmnptuvxBCHP] [-o option-name] [arg ...]",
      "documentation": { "value": "Sets or unsets shell options and positional parameters." },
      "parameters": [
        { "label": "-e", "documentation": { "value": "Exit immediately if a command exits with a non-zero status" } },
        { "label": "-u", "documentation": { "value": "Treat unset variables as an error during parameter expansion" } },
        { "label": "-x", "documentation": { "value": "Print commands and their arguments as they are executed" } },
        { "label": "-o option-name", "documentation": { "value": "Set a named option like `pipefail`, `nounset`, `errexit`" } }
      ]
    },
    {
      "label": "shift [n]",
      "documentation": { "value": "Shifts positional parameters to the left by n positions (default 1)." },
      "parameters": [
        { "label": "n", "documentation": { "value": "The number of positions to shift (default is 1)" } }
      ]
    },
    {
      "label": "getopts optstring name [args]",
      "documentation": { "value": "Parses positional parameters as options. Used in while loops for option processing." },
      "parameters": [
        { "label": "optstring", "documentation": { "value": "The string of recognized option letters; a colon after a letter means it takes an argument" } },
        { "label": "name", "documentation": { "value": "Variable name to store the current option letter" } }
      ]
    },
    {
      "label": "basename string [suffix]",
      "documentation": { "value": "Strips the directory and optionally the suffix from a file path." },
      "parameters": [
        { "label": "string", "documentation": { "value": "The file path" } },
        { "label": "suffix", "documentation": { "value": "An optional suffix to remove from the result" } }
      ]
    },
    {
      "label": "dirname string",
      "documentation": { "value": "Strips the last component from a file path, returning the directory portion." },
      "parameters": [
        { "label": "string", "documentation": { "value": "The file path" } }
      ]
    }
  ]
}
