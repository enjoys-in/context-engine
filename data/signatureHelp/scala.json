{
  "language": "scala",
  "triggerCharacters": ["(", ","],
  "retriggerCharacters": [","],
  "signatures": [
    {
      "label": "println(x: Any): Unit",
      "documentation": { "value": "Prints a value to the standard output followed by a newline." },
      "parameters": [
        { "label": "x", "documentation": { "value": "The value to print" } }
      ]
    },
    {
      "label": "List.map[B](f: A => B): List[B]",
      "documentation": { "value": "Builds a new list by applying a function to all elements of this list." },
      "parameters": [
        { "label": "f", "documentation": { "value": "The function to apply to each element" } }
      ]
    },
    {
      "label": "List.filter(p: A => Boolean): List[A]",
      "documentation": { "value": "Selects all elements of this list which satisfy a predicate." },
      "parameters": [
        { "label": "p", "documentation": { "value": "The predicate used to test elements" } }
      ]
    },
    {
      "label": "List.foldLeft[B](z: B)(op: (B, A) => B): B",
      "documentation": { "value": "Applies a binary operator to a start value and all elements of this list, going left to right." },
      "parameters": [
        { "label": "z", "documentation": { "value": "The start/initial value" } },
        { "label": "op", "documentation": { "value": "The binary operator to apply" } }
      ]
    },
    {
      "label": "Option.getOrElse[B >: A](default: => B): B",
      "documentation": { "value": "Returns the option's value if it is nonempty, or the default value otherwise." },
      "parameters": [
        { "label": "default", "documentation": { "value": "The default value to return if the option is `None`" } }
      ]
    },
    {
      "label": "Future.apply[T](body: => T)(implicit executor: ExecutionContext): Future[T]",
      "documentation": { "value": "Starts an asynchronous computation and returns a `Future` object with the result." },
      "parameters": [
        { "label": "body", "documentation": { "value": "The asynchronous computation to execute" } },
        { "label": "executor", "documentation": { "value": "The implicit execution context for the computation" } }
      ]
    },
    {
      "label": "Try.apply[T](r: => T): Try[T]",
      "documentation": { "value": "Constructs a `Try` using the by-name parameter. Returns `Success` or `Failure`." },
      "parameters": [
        { "label": "r", "documentation": { "value": "The computation that may throw an exception" } }
      ]
    },
    {
      "label": "Map.getOrElse[V1 >: V](key: K, default: => V1): V1",
      "documentation": { "value": "Returns the value associated with a key, or a default value if the key is not contained in the map." },
      "parameters": [
        { "label": "key", "documentation": { "value": "The key to look up" } },
        { "label": "default", "documentation": { "value": "A computation of the default value" } }
      ]
    },
    {
      "label": "String.format(args: Any*): String",
      "documentation": { "value": "Uses this string as a format string, returning a string obtained by substituting the given arguments." },
      "parameters": [
        { "label": "args", "documentation": { "value": "The arguments referenced by the format specifiers" } }
      ]
    },
    {
      "label": "Array.fill[T](n: Int)(elem: => T): Array[T]",
      "documentation": { "value": "Returns an array that contains the results of some element computation a number of times." },
      "parameters": [
        { "label": "n", "documentation": { "value": "The number of elements in the array" } },
        { "label": "elem", "documentation": { "value": "The element computation" } }
      ]
    },
    {
      "label": "Set.contains(elem: A): Boolean",
      "documentation": { "value": "Tests if some element is contained in this set." },
      "parameters": [
        { "label": "elem", "documentation": { "value": "The element to test for membership" } }
      ]
    },
    {
      "label": "Range.apply(start: Int, end: Int, step: Int): Range",
      "documentation": { "value": "Creates a range of integers from start (inclusive) to end (exclusive) with the given step." },
      "parameters": [
        { "label": "start", "documentation": { "value": "The start value of the range (inclusive)" } },
        { "label": "end", "documentation": { "value": "The end value of the range (exclusive)" } },
        { "label": "step", "documentation": { "value": "The step value between elements" } }
      ]
    }
  ]
}
