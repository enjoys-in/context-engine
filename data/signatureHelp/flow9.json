{
  "language": "flow9",
  "triggerCharacters": ["(", ","],
  "retriggerCharacters": [","],
  "signatures": [
    {
      "label": "println(s: string): void",
      "documentation": { "value": "Prints a string followed by a newline to standard output." },
      "parameters": [
        { "label": "s", "documentation": { "value": "The string to print." } }
      ]
    },
    {
      "label": "strlen(s: string): int",
      "documentation": { "value": "Returns the length of a string in characters." },
      "parameters": [
        { "label": "s", "documentation": { "value": "The string to measure." } }
      ]
    },
    {
      "label": "strReplace(s: string, old: string, new: string): string",
      "documentation": { "value": "Replaces all occurrences of a substring within a string." },
      "parameters": [
        { "label": "s", "documentation": { "value": "The original string." } },
        { "label": "old", "documentation": { "value": "The substring to search for." } },
        { "label": "new", "documentation": { "value": "The replacement substring." } }
      ]
    },
    {
      "label": "i2s(i: int): string",
      "documentation": { "value": "Converts an integer to its string representation." },
      "parameters": [
        { "label": "i", "documentation": { "value": "The integer to convert." } }
      ]
    },
    {
      "label": "s2i(s: string): int",
      "documentation": { "value": "Parses a string as an integer. Returns 0 if parsing fails." },
      "parameters": [
        { "label": "s", "documentation": { "value": "The string to parse." } }
      ]
    },
    {
      "label": "map(a: [?], fn: (?) -> ??): [??]",
      "documentation": { "value": "Applies a function to each element of an array and returns a new array of results." },
      "parameters": [
        { "label": "a", "documentation": { "value": "The input array." } },
        { "label": "fn", "documentation": { "value": "The function to apply to each element." } }
      ]
    },
    {
      "label": "filter(a: [?], fn: (?) -> bool): [?]",
      "documentation": { "value": "Returns a new array containing only elements for which the predicate returns true." },
      "parameters": [
        { "label": "a", "documentation": { "value": "The input array." } },
        { "label": "fn", "documentation": { "value": "The predicate function." } }
      ]
    },
    {
      "label": "fold(a: [?], init: ??, fn: (??, ?) -> ??): ??",
      "documentation": { "value": "Reduces an array to a single value by applying a function with an accumulator." },
      "parameters": [
        { "label": "a", "documentation": { "value": "The input array." } },
        { "label": "init", "documentation": { "value": "The initial accumulator value." } },
        { "label": "fn", "documentation": { "value": "The reducer function taking accumulator and element." } }
      ]
    },
    {
      "label": "isSameObj(a: ?, b: ?): bool",
      "documentation": { "value": "Checks whether two values reference the same object in memory." },
      "parameters": [
        { "label": "a", "documentation": { "value": "The first value." } },
        { "label": "b", "documentation": { "value": "The second value." } }
      ]
    },
    {
      "label": "ref(value: ?): ref<?>",
      "documentation": { "value": "Creates a mutable reference cell containing the given value." },
      "parameters": [
        { "label": "value", "documentation": { "value": "The initial value for the reference." } }
      ]
    }
  ]
}
