{
  "language": "json",
  "definitions": {
    "object": {
      "signature": "{ \"key\": value }",
      "description": "Unordered name/value pairs. Duplicate keys are undefined behaviour.",
      "type": "type",
      "module": "RFC 8259"
    },
    "array": {
      "signature": "[ value, ... ]",
      "description": "Ordered, heterogeneous list.",
      "type": "type",
      "module": "RFC 8259"
    },
    "string": {
      "signature": "string",
      "description": "Double-quoted UTF-8. Single quotes are invalid, as are trailing commas and comments.",
      "type": "type",
      "module": "RFC 8259"
    },
    "number": {
      "signature": "number",
      "description": "JSON's only numeric type — no distinction between integer and float, and no NaN or Infinity.",
      "type": "type",
      "module": "RFC 8259"
    },
    "boolean": {
      "signature": "true | false",
      "description": "Lowercase only.",
      "type": "type",
      "module": "RFC 8259"
    },
    "null": {
      "signature": "null",
      "description": "JSON's empty value. Lowercase only.",
      "type": "type",
      "module": "RFC 8259"
    },
    "key": {
      "signature": "\"key\": value",
      "description": "Object keys must be double-quoted strings. Each key in an object should be unique.",
      "type": "syntax",
      "module": "JSON RFC 8259"
    },
    "escape_sequences": {
      "signature": "\\\" \\\\ \\/ \\b \\f \\n \\r \\t \\uXXXX",
      "description": "Special characters in strings are represented with backslash escape sequences.",
      "type": "syntax",
      "module": "JSON RFC 8259"
    },
    "json_schema": {
      "signature": "$schema",
      "description": "JSON Schema is a vocabulary for annotating and validating JSON documents. The $schema keyword declares which version of the JSON Schema spec the document uses.",
      "type": "standard",
      "module": "JSON Schema"
    },
    "json_pointer": {
      "signature": "/path/to/value",
      "description": "JSON Pointer (RFC 6901) is a syntax for identifying a specific value within a JSON document using a path of property names and array indices.",
      "type": "standard",
      "module": "RFC 6901"
    },
    "mime_type": {
      "signature": "application/json",
      "description": "The official MIME type for JSON is application/json. The file extension is .json.",
      "type": "metadata",
      "module": "RFC 8259"
    },
    "trailing_comma": {
      "signature": "{ \"a\": 1, } // INVALID",
      "description": "Trailing commas are NOT allowed in standard JSON. This is a common error when editing JSON files.",
      "type": "syntax",
      "module": "JSON RFC 8259"
    },
    "comments": {
      "signature": "// or /* */ - NOT SUPPORTED",
      "description": "Standard JSON does not support comments. Some tools (JSONC, JSON5) extend JSON to allow them. Use _comment keys as a workaround.",
      "type": "syntax",
      "module": "JSON RFC 8259"
    },
    "$schema": {
      "signature": "\"$schema\": \"<uri>\"",
      "description": "Points at a JSON Schema so editors can validate and complete the document.",
      "type": "keyword",
      "module": "JSON Schema"
    }
  }
}
