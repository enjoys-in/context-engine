{
  "language": "redis",
  "typeDefinitions": {
    "string": {
      "type": "string",
      "description": "string type in Redis"
    },
    "list": {
      "type": "list",
      "description": "list type in Redis"
    },
    "set": {
      "type": "set",
      "description": "set type in Redis"
    },
    "sorted set": {
      "type": "sorted set",
      "description": "sorted set type in Redis"
    },
    "hash": {
      "type": "hash",
      "description": "hash type in Redis"
    },
    "stream": {
      "type": "stream",
      "description": "stream type in Redis"
    },
    "bitmap": {
      "type": "bitmap",
      "description": "bitmap type in Redis"
    },
    "hyperloglog": {
      "type": "hyperloglog",
      "description": "hyperloglog type in Redis"
    },
    "geospatial": {
      "type": "geospatial",
      "description": "geospatial type in Redis"
    }
  }
}
